# SAM 3 image inference runtime. Torch/torchvision and the pinned SAM 3 source
# checkout are installed separately by Dockerfile.segmenter.
fastapi==0.116.1
einops==0.8.1
ftfy==6.1.1
huggingface-hub==0.36.0
iopath==0.1.10
numpy==1.26.4
Pillow==11.3.0
psutil==7.0.0
pycocotools==2.0.10
python-multipart==0.0.20
regex==2024.11.6
# sam3.model_builder imports pkg_resources; setuptools 81+ removes it.
setuptools==80.9.0
timm==1.0.22
tqdm==4.67.1
typing-extensions==4.12.2
uvicorn==0.35.0
